<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.kotlinmessenger">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Global Messenger"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">

        <activity android:name=".ChatLogActivity"
            android:parentActivityName=".LastMessagesActivity"
            android:label="username">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.kotlinmessenger.LastMessagesActivity" />
        </activity>

        <activity
            android:name=".FindUserActivity"
            android:label="Find user"
            android:parentActivityName=".LastMessagesActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.kotlinmessenger.LastMessagesActivity" />
        </activity>

        <activity
            android:name=".LastMessagesActivity"
            android:label="Last messages" />
        <activity android:name=".SignInActivity"/>

        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SignUpActivity"
            android:parentActivityName=".SignInActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.kotlinmessenger.SignInActivity" />
        </activity>

    </application>
</manifest>